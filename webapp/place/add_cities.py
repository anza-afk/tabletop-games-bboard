# from webapp.database import db_session
# from webapp.place.models import Country, City

# if __name__ == "__main__":
#     with db_session as session:
#         session.add(Country(name='Россия'))
#         session.commit()
